<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.colabear754.swagger_example.mapper.MemberMapper">
    <select id="getDocuments" resultType="DocumentDTO">
        select a.*
        from (
            select rownum r, b.*
            from (
                select v_board.*
                from v_board
                order by v_board.seq desc
            ) b
        ) a
        <![CDATA[where r >= 1 and r <= 5]]>
    </select>
</mapper>